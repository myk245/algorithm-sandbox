// N = integer representing size of grid
// lamps = list of L entries; each entry is a list with 2 entries representing the x, y coordinates of a lamo
// M = list of M entrues; each entry is a list of 2 entries representing the x, y coordinates of a query cell
// output should be a list of M entries; each entry will either be "LIGHT" or "DARK"

// very difficult - will need to come back to this
function checkIllumination(N, lamps, queries) {
   // keep track of illuminated cells: row, column, and both diagonals
   let rowCells = {};
   let columnCells = {};
   let forwardDiagonalCells = {};
   let backwardDiagonalCells = {};
}